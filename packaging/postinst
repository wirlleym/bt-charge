#!/bin/sh
set -e

# Atualiza o cache de ícones do hicolor (o ícone do 🎧 é instalado lá).
if command -v gtk-update-icon-cache >/dev/null 2>&1; then
    gtk-update-icon-cache -f -q /usr/share/icons/hicolor 2>/dev/null || true
fi

exit 0
